import path from 'path';

export const BASE_DIR = path.join(process.cwd(), 'contents');
export const BLOG_DIR = path.join(process.cwd(), 'contents', 'articles');
export const PROJECT_DIR = path.join(process.cwd(), 'contents', 'projects');
export const NOTE_DIR = path.join(process.cwd(), 'contents', 'notes');
